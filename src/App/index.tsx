import React, { FC } from 'react'
import { render } from 'react-dom'

const Root: FC = () => {
    return <div style={{ color: '#fff' }}>Pogfssssss</div>
}

render(<Root />, document.getElementById('root'))
